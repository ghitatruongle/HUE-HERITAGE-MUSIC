class AppConstants {
  static const String appName = 'Hue Heritage Music';
  static const String appVersion = '0.0.0-alpha2';
  static const Duration apiTimeout = Duration(seconds: 30);
  static const Duration dspTimeout = Duration(minutes: 15);
}
